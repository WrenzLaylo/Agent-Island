# v0.3.0 changes

- Added a Codex `terminal-known` adapter for managed Agent Island sessions.
- Detects current Codex command-execution and file-change approval prompts.
- Surfaces only the decisions present in the live Codex prompt.
- Maps one-time approval, persistent “don’t ask again”, and denial back to the Codex terminal.
- Adds stale-prompt checks so scrollback is not treated as a live approval.
- Adds a stronger warning before persistent Codex command-prefix approval.
- Keeps Claude terminal-only and documents the future structured Codex app-server path.
