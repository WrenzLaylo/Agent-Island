# v0.2.3 changes

- Made side docking more forgiving so dragging near a screen edge reliably snaps into the docked orb.
- Strengthened the docked idle styling so the side state remains a true circle instead of visually collapsing into a pill.
