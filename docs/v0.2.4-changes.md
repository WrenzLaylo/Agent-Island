# v0.2.4 changes

- Fixed `Invalid island size` when the quiet idle island docks as a 48 × 48 px orb.
- Centralised the renderer window size limits so future visual sizes and IPC validation stay aligned.
- Improved resize validation errors to include the requested and allowed dimensions.
